using UnityEditor;
using UnityEngine;

namespace Assets.Scripts.Entities
{
    public class Actor : Entity
    {
        public Actor(IDGenerator idGenerator) : base(idGenerator)
        {
        }
    }
}